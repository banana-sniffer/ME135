	# cir = Circle(Point(x, y), radius)
		# cir.setOutline('blue')
		# cir.setFill('blue')
		# rect = Rectangle(Point(x-(radius+5),375-5),Point(x+(radius+5),375+5))
		# rect.setOutline('red')
		# rect.setFill('red')
		# cir.draw(win)
		# rect.draw(win)
		# x_vals.append(x)
		# y_vals.append(336-y)
		# li.set_xdata(x_vals)
		# li.set_ydata(y_vals)
		
		# For all three subplots
		# t.append(val)
		# val = val + 1
		# jk.set_xdata(t)
		# jk.set_ydata(y_vals)
		# radii.append(radius)
		# hl.set_xdata(t)
		# hl.set_ydata(radii)
		# ax.relim()
		# ax.autoscale_view(True,True,False)
		# ax1.relim()
		# ax1.autoscale_view(True,True,False)
		# ax2.relim()
		# ax2.autoscale_view(True,True,False)
		# fig.canvas.draw()


		# Setting up the animation 
# win = GraphWin('Bouncing Ball', 700, 500)

# set up plot for dynamic plotting
# figure(num=None, figsize=(6, 6), dpi=80, facecolor='w', edgecolor='k')
# t = []
# fig = plt.figure()
# ax = fig.add_subplot(111)
# x_vals = []
# y_vals = []
# li, = ax.plot(x_vals)
# # ax.relim()
# # ax.autoscale_view(True,True,False)
# ax.set_xlim([-50,566])
# ax.set_ylim([0,336])

# For all three subplots
# ax2 = fig.add_subplot(312)
# y_vals = []
# jk, = ax2.plot(y_vals)
# ax2.set_ylim([0,336])

# ax1 = fig.add_subplot(313)
# radii = []
# val = 0
# ax1.set_ylim([0,150])
# hl, = ax1.plot(radii)

# fig.canvas.draw()
# plt.show(block=False)